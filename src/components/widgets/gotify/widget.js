const widget = {
  api: "{url}/{endpoint}",
  mappings: {
    application: {
      endpoint: "application"
    },
    client: {
      endpoint: "client"
    },
    message: {
      endpoint: "message"
    }
  }
};

export default widget;
