const widget = {
  api: "{url}/api/v2.0/{endpoint}?apikey={key}&configured=true",
  loginURL: "{url}/UI/Dashboard",

  mappings: {
    indexers: {
      endpoint: "indexers"
    }
  }
};

export default widget;
