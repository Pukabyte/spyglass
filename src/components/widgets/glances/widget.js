const widget = {
  api: "{url}/api/4/{endpoint}",
  mappings: {
    quicklook: { endpoint: "quicklook" },
    mem: { endpoint: "mem" },
    fs: { endpoint: "fs" },
    cpu: { endpoint: "cpu" },
    system: { endpoint: "system" },
  },
};

export default widget;
