const widget = {
  api: "{url}/api/{endpoint}",
  mappings: {
    "server/statistics": {
      endpoint: "server/statistics"
    },
    "server/version": {
      endpoint: "server/version"
    }
  }
};

export default widget;
