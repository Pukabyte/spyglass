const widget = {
  api: "{url}/api/{endpoint}",
  mappings: {
    livedata: {
      endpoint: "livedata/status",
    },
    system: {
      endpoint: "system/status",
    },
  },
};

export default widget;
