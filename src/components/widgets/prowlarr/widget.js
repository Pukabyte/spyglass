const widget = {
  api: "{url}/api/v1/{endpoint}?apikey={key}",
  mappings: {
    indexer: {
      endpoint: "indexer"
    },
    indexerstats: {
      endpoint: "indexerstats"
    }
  }
};

export default widget;
